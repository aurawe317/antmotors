{
  "name": "antmotors-sync",
  "version": "1.0.0",
  "private": true,
  "description": "ANT MOTORS PWA + multi-tenant sync server (serves frontend and API from one port)",
  "main": "server/server.js",
  "scripts": {
    "start": "node --experimental-sqlite server/server.js",
    "dev": "node --experimental-sqlite server/server.js",
    "test": "node server/test-auth.js"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
