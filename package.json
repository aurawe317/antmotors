{
  "name": "antmotors",
  "version": "1.0.0",
  "private": true,
  "description": "ANT MOTORS PWA + multi-tenant sync server / Cloudflare Pages Functions backend (Supabase)",
  "main": "server/server.js",
  "scripts": {
    "start": "node --experimental-sqlite server/server.js",
    "dev": "node --experimental-sqlite server/server.js",
    "test": "node server/test-auth.js"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
